{
  "name": "taj-gold-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker that scrapes dewanaldahab gold prices and serves clean JSON",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
